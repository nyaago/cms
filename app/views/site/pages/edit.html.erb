<h1>
  <%= PageArticle.human_name %>の編集
</h1>

<% if @article %>
<%= render 'form' %>
<% else %>  
  <p class='not_found'>
    指定された<%= PageArticle.human_name %>はみつかりません
  </p>
<% end %>

<div class="actions" id="back_action">
<%= link_to '一覧へ戻る', url_for(:back) %>
</div>