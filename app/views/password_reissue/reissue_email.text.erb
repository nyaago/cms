<%= @user.login %>様
以下のURLにアクセスしてパスワードを再発行してください。
操作は、<%= @expiration_minute %> 分以内に完了してください。
<%= @request.protocol + @request.host_with_port + 
url_for(:controller => 'site/password_reissue', :action => :edit, 
:reissue_password => @user.reissue_password)  %>